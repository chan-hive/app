query threadList($count: Int!, $before: DateTime) {
    threadCount
    threads(count: $count, before: $before) {
        id
        board {
            id
            title
            isWorkSafe
        }
        postCount
        fileCount
        createdAt
        opPost {
            id
            title
            content
            createdAt
            file {
                ...FileInformation
            }
        }
    }
}
